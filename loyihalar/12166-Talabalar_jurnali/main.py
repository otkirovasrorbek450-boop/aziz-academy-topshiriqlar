# Talabalar jurnali
# Buyruqlar: add <ism> <ball>, list, top, search <ism>, stat, exit
jurnal = []   # har element: [ism, ball]
while True:
    cmd = input().strip()
    if cmd == 'exit':
        break
    # TODO: buyruqlarni shu yerda qayta ishlang
